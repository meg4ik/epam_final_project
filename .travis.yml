language: python

python:
  - 3.8

services:
  - mysql

install: pip install -r requirements.txt pytest-cov python-coveralls

before_script:
  - mysql -e "CREATE DATABASE department_project;"

script: pytest

after_success: coveralls -i
